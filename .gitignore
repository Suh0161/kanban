# Dependencies
node_modules/

# Build output
dist/
build/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local databases / WAL files (NEVER commit user data)
database/*.db
database/*.db-shm
database/*.db-wal
/*.db
/*.db-shm
/*.db-wal

# Repo-root migration / ops artifacts (Fly→Railway copies — never commit)
/*.b64
/uploads/
/uploads.tgz
/avatar.png

# Environment files (secrets)
.env
.env.*
!.env.example
!frontend/.env.example
!backend/.env.example
!website/.env.example

# Uploads (user content)
backend/uploads/*
!backend/uploads/.gitkeep

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Local-only scripts
_replace.ps1

# Throwaway scratch tests (see AGENTS.md "Verify with a throwaway test")
tmp-test-*
*.scratch.test.*
*.scratch.*

# Marketing site (separate private repo — keep a local copy out of git)
website/
