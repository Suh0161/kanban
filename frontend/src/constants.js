export const PRIORITIES = ['Critical', 'High', 'Medium', 'Low'];

// Empty initial state — real data always comes from the API
export const initialData = {
  tasks: {},
  columns: {},
  columnOrder: [],
  customFields: [],
  labels: [],
};
